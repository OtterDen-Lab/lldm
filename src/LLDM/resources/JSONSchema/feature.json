{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "feature.json",
    "title": "JSON Schema for D&D 5e feature.",
    "description": "This schema describes a character feature, not to be confused with feat usually a class or racial ability",
    "type": "object",
    "required": ["name"],
    "properties": {
        "name": {
            "type": "string"
        },
        "description": {
            "type": "string",
            "input": "textarea"
        },
        "source": {
            "$ref": "source.json"
        }
    }
}